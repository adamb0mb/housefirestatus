name = "housefirestatus"
main = "worker/index.js"
compatibility_date = "2026-08-01"

[assets]
directory = "./public"
binding = "ASSETS"
# API routes are handled by the Worker; everything else falls through to static files.
run_worker_first = ["/api/*"]

[observability]
enabled = true
